{% extends "base.html.twig" %}

{% block body %}
    {% include 'flash.html.twig' %}
<fieldset>
    <legend>Location</legend>
    <div class="row">
        <form class="form-horizontal" id="form-location" role="form" method="post" action="{{ path('location_create') }}">

            <div class="col-md-12">
                <div class="portlet-body">

                    <div class="form-body">
                        <div class="form-group">
                            <label class="col-md-3 col-sm-3 control-label">{{ form_label(form.name) }}</label>
                            <div class="col-md-5 col-sm-5">
                                {{ form_widget(form.name) }}
                                <span class="help-block">{{ form_errors(form.name) }}</span>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-md-3 col-sm-3 control-label">{{ form_label(form.geocode) }}</label>
                            <div class="col-md-5 col-sm-5">
                                {{ form_widget(form.geocode) }}
                                <span class="help-block">{{ form_errors(form.geocode) }}</span>
                            </div>
                        </div>
                    </div>
                    {{ form_row(form._token) }}
                    <div class="form-actions">
                        <div class="row">
                            <div class="col-md-offset-3 col-md-9">
                                <button type="submit" id="location-submit" class="btn green">Save</button>
                            </div>
                        </div>
                    </div>

                </div>
            </div>

        </form>
    </div>
    </fieldset>
<script type="text/javascript">

</script>



{% endblock %}

{# {% block javascriptBlock %}

     {{ parent() }}

     {% javascripts
     '@BundleAppBundle/Resources/public/js/CategoryFormValidation.js'
     %}
     <script type="text/javascript" src="{{ asset_url }}"></script>
     {% endjavascripts %}

 {% endblock javascriptBlock %}#}
