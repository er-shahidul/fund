<link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
<div class="row">
        <form class="form-horizontal" id="form-campaign" role="form" method="post" action="{{ path('campaign_user-profile-verify') }}">

            <div class="col-md-12">
                <div class="portlet-body">

                    <div class="form-body">
                        <div class="form-group">
                            <label class="col-md-3 col-sm-3 control-label">{{ form_label(form.email) }}</label>
                            <div class="col-md-5 col-sm-5">
                                {{ form_widget(form.email) }} <a href="#" class="verifyEmail">verify</a>
                                <span class="help-block"></span>
                            </div>

                        </div>
                        <div class="form-group">
                            <label class="col-md-3 col-sm-3 control-label">{{ form_label(form.profile.PhoneNumber) }}</label>
                            <div class="col-md-5 col-sm-5">
                                {{ form_widget(form.profile.PhoneNumber) }} <a href="#" class="verifyPhoneNumber">verify</a>
                                <span class="help-block"></span>
                            </div>
                        </div>

                    </div>
                    {{ form_row(form._token) }}
                    <div class="form-actions">
                        <div class="row">
                            <div class="col-md-offset-3 col-md-9">
                                {#<a class="btn default" href="{{ path('courtfee_list') }}">Cancel</a>#}
                                <button type="submit" id="campaign-submit" class="btn green">Save</button>
                            </div>
                        </div>
                    </div>

                </div>
            </div>

        </form>
    </div>

